spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_NAME}
    password: ${DATABASE_PASS}

ocrBaseUrl: "http://tesseract:8884/tesseract"

policy:
  service:
    base-url: http://policy-management-service:8080/employee/policy_limit